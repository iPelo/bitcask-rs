# Benchmarks

Benchmark notes and results will live here.

Planned comparison targets:

- `bitcask-rs`
- `sled`
- `rocksdb`

All benchmark reports should include hardware, Rust version, workload shape,
value sizes, durability mode, and whether data was already warm in page cache.

